import React, { Component, PropTypes } from 'react';

class Actor extends Component {
    render () {
        return (
            <div>
                Actor
            </div>
        );
    };
}

export default Actor;