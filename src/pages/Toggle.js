import React, { Component } from 'react';

class Toggle extends Component {
    render () {
        return (
                <div>
                    Act as logged in
                    <input type="checkbox" />
                </div>
        );
    }
};

export default Toggle;